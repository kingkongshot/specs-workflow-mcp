# 测试套件环境变量配置
# 这是测试环境的唯一真相源

# 基础路径配置
TEST_ROOT_DIR=/path/to/your/specs-mcp/test-specs
PROJECT_ROOT_DIR=/path/to/your/specs-mcp
MCP_SERVER_PATH=/path/to/your/specs-mcp/dist/index.js

# 测试运行配置
TEST_WORK_DIR=/path/to/your/specs-mcp/test-specs
TEST_FIXTURES_DIR=/path/to/your/specs-mcp/test-specs/fixtures
TEST_CASES_DIR=/path/to/your/specs-mcp/test-specs/test-cases
TEST_REPORTS_DIR=/path/to/your/specs-mcp/test-specs/reports
TEST_TEMP_DIR=/path/to/your/specs-mcp/test-specs/temp

# MCP 服务器配置
MCP_SERVER_CWD=/path/to/your/specs-mcp/test-specs
MCP_SERVER_TIMEOUT=10000
MCP_PROTOCOL_VERSION=0.1.0

# 测试行为配置
TEST_CLEANUP_AFTER_RUN=true
TEST_VERBOSE_MODE=false
TEST_PARALLEL_EXECUTION=false
TEST_MAX_RETRIES=0

# 测试报告配置
TEST_REPORT_FORMAT=markdown
TEST_REPORT_INCLUDE_DETAILS=true
TEST_REPORT_TIMESTAMP_FORMAT=YYYY-MM-DD-HH-mm-ss

# 调试配置
TEST_DEBUG_MODE=false
TEST_LOG_LEVEL=info
TEST_CAPTURE_MCP_LOGS=false

# 测试过滤配置
# 可以通过命令行参数覆盖
TEST_FILTER_GROUP=
TEST_FILTER_NAME=
TEST_FILTER_PRIORITY=

# 测试超时配置（毫秒）
TEST_CASE_TIMEOUT=30000
TEST_SETUP_TIMEOUT=5000
TEST_CLEANUP_TIMEOUT=5000